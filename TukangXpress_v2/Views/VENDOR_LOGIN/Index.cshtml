@model IEnumerable<TukangXpress_v2.Models.VENDOR_LOGIN>

@{
    ViewBag.Title = "Index";
}

<h2>Vendor Login</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Login ID
        </th>
        <th>
            Email
        </th>
        <th>
            Password
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.vendorlogin_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vendor_email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vendor_password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.vendorlogin_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.vendorlogin_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.vendorlogin_ID })
        </td>
    </tr>
}

</table>
